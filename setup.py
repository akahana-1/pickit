from setuptools import setup, find_packages
version = '1.0.2'

setup(
        name='pickit',
        version=version,
        packages=['pickit'],
        description="Simple pickle iteration tool.",
        )
